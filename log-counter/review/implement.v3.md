# 実装レビュー（再レビュー 2）

## 判定

- 現在の判定：差し戻し
- 次の作業：I-02 の許可レベルのインデントを設計どおり半角スペース2つにする。I-03 の行一覧を書き出す `writeln!` の結果も `?` で伝播する。

## 確認結果

- `cargo fmt --check`：成功
- `cargo clippy -- -D warnings`：成功
- `cargo test`：成功（テストは0件）
- `cargo run`：正常ログで `INFO: 2`、`WARN: 1`、`ERROR: 1` を出力

## 採点

| 観点                 |    配点 |   得点 | 根拠                                                                                                    |
| -------------------- | ------: | -----: | ------------------------------------------------------------------------------------------------------- |
| 設計・要求への適合   |      25 |     22 | 区切り行が追加され、ほぼ設計どおり。許可レベルのインデントがタブであり、設計の半角スペース2つと異なる。 |
| 機能の正確性         |      30 |     25 | 正常な3レベル、空行の無視、未知レベルの収集と報告を実装している。                                       |
| エラー処理・境界条件 |      20 |     13 | 見出し・末尾の書き込みは `?` で伝播するが、各未知レベルの `writeln!` の結果を捨てている。               |
| 可読性・Rust らしさ  |      15 |     12 | 責務が分離され、禁止されている `unwrap` 等はない。公開型・公開関数の rust-doc は未記載。                |
| 実行確認の妥当性     |      10 |      6 | 正常系の実行と静的検査を確認した。境界ケースの体系的な検証は次のテスト工程で扱う方針とする。            |
| **合計**             | **100** | **78** | 差し戻し。                                                                                              |

## 指摘と回答

| ID   | ステータス | 指摘箇所                                 | 質問・指摘内容                                                                       | 人間の回答                                                                                               | AI の確認                                                                                                                                                                                                      |
| ---- | ---------- | ---------------------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I-01 | 承認       | `src/log.rs` の `aggregate`              | 未知レベルの行番号を設計どおり1始まりにする。                                        | 修正しました．                                                                                           | `i + 1` を渡す実装を確認した。                                                                                                                                                                                 |
| I-02 | 差し戻し   | `src/log.rs` の `UnknownLevelError::fmt` | 設計のエラー表示に `***` 区切りと、許可レベルの半角スペース2つのインデントを含める。 | 修正しました．．                                                                                         | `***` の2行は追加済み。ただし現在は `\tINFO, WARN, ERROR` であり、設計の `  INFO, WARN, ERROR` と異なる。                                                                                                      |
| I-03 | 差し戻し   | `src/log.rs` の `UnknownLevelError::fmt` | フォーマッタへのすべての書き込み失敗を呼び出し元へ返す。                             | `?` により簡易的にエラー伝播を実現するよう修正しました。修正しました．                                   | 見出し・末尾は `?` で伝播するが、ループ内は `_ = writeln!(...)` で結果を捨てている。前回この行を見落として承認したのはレビュー側の誤りだった。`writeln!(f, "{}", item.display())?;` の形で伝播させてください。 |
| I-04 | 却下       | 実行確認                                 | 空行・未知レベルを含む入力の実行結果を記録する。                                     | 実装フェーズは，正常系がある程度動作することまで確認すればいいと思います．テストフェーズで行うことです． | 理由を確認した。境界ケースを網羅的に検証する責務は、次のテストケース設計・テスト実装工程で扱う。ここでは正常系の実行確認までとする。                                                                           |
