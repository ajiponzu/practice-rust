# 設計レビュー

## 判定

- 現在の判定：差し戻し
- 次の作業：選択した「統合テストなし」の方針に合わせ、モジュール構成と利用例を有効な Rust の構成へ修正してから再レビューを依頼する。

## 採点

| 観点 | 配点 | 得点 | 根拠 |
| --- | ---: | ---: | --- |
| 要求・受け入れ条件の明確さ | 25 | 24 | 空白区切り、レベルのみの行、未知レベルの位置（1始まり）が明確。 |
| API・構造の妥当性 | 25 | 17 | `Vec<String>` を入力とする `aggregate` と結果・エラー型を定義できた。ただし `src/lib.rs` がない構成で `use log_counter::log` は成立しない。 |
| エラー処理・制約の考慮 | 20 | 19 | 未知レベルをまとめて返し、許可レベルも示す方針が明確。`std::error::Error` の実装対象を明記するとよい。 |
| テスト可能性 | 20 | 16 | `aggregate` を分離しているため単体テスト可能。詳細ケースは次工程へ回す判断は妥当だが、統合テストをしないなら `tests/` とファイル読込関数の記述は不要。 |
| 文書・図の明確さ | 10 | 9 | 主な設計判断を確認できた。構成図と API 利用例の不一致だけを解消すればよい。 |
| **合計** | **100** | **85** | 基準点には達したが、必須条件である実装可能なモジュール構成が未充足。 |

## 指摘と回答

| ID | ステータス | 指摘箇所 | 質問・指摘内容 | 人間の回答 | AI の確認 |
| --- | --- | --- | --- | --- | --- |
| D-01 | 承認 | API・利用例 / データと状態 | 関数・型・`HashMap` 不使用の方針を定義してください。 | LevelCounterの定義を記載しました．HashMapを使っていません | 確認済み。 |
| D-02 | 承認 | エラー処理 / 未決事項 | レベルのみの行と、エラー位置の扱いを決めてください。 | 記載しました | 行番号を1始まりとする方針を確認しました。 |
| D-03 | 承認 | テスト可能性 | 単体・統合テストの対象と期待結果を具体例で追記してください。 | 統合テストを実装する規模ではなかったので統合テストについては削除しました．テスト対象はテストケース設計フェーズで扱います． | 妥当です。具体的なテストケースは次工程で扱います。 |
| D-04 | 差し戻し | モジュール構成 / API・利用例 / テスト可能性 | 統合テストを行わず `src/lib.rs` を作らない方針なら、構成から `tests/` を削除し、`main.rs` に `mod log;` を宣言して `log::aggregate(logs)` と呼ぶ利用例へ直してください。 | `Vec<String>` と空白区切りに統一しました。統合テストは不要なので `src/lib.rs` は作りません。位置は1始まりです。 | 利用例と構成がまだ方針に反しています。 |
